console.log('the file color was changed to '+color+', we should perform an ajax call to update it in the file storage!');
console.log('however, as it is user specific, different people probably want to set different colors so a better solution');
console.log('to not bind this to per-file, but per file/user combination...');
console.log('file:');
console.log(file);
console.log('dom object:');
console.log(domObj);
console.log('----');